## Learned User Preferences

- Prefer `velumia-pm` as a proper git submodule at `velumia-pm/`, not a sibling symlink.
- Create Sprint PRD and Implementation Spec **in ChatPRD** (MCP `create_document` / `update_document`); Implementation Spec **must** use [`templates/chatprd/chatprd_feature-implementation-spec.tpl.md`](./templates/chatprd/chatprd_feature-implementation-spec.tpl.md) as the section outline. Do not author local `sprint-prd.md` or `implementation-plan.md` first — local files are mirrors synced via `velumia-planning-chatprd-sync`.
- Prefer Nx-native dev commands (`pnpm nx run ui:dev`, `pnpm nx run desktop:dev`); no root npm/pnpm script aliases.
- Implementation Specs must declare shared code placement in `libs/ui/*` or `libs/desktop/*`, or explicitly inline-in-app; do not create `libs/*` Nx packages without an explicit spec subtask.
- `apps/ui` must work in both Tauri desktop and standalone Vite web; `apps/ui/src/lib/api.ts` routes to Tauri `invoke` when the bridge is present, otherwise `webInvoke` in `web-api.ts`.
- Match Node and pnpm versions between local dev and CI — pin exact versions (no loose majors in CI).
- Prompt library detail defaults to **read view**; **edit icon** beside title toggles edit mode (title, tags, content).
- Unsaved exit from prompt edit shows **confirm dialog** with **Save**, **Discard**, and **Save draft** options.
- Primary prompt save control is labeled **Update prompt**; disabled when nothing changed.

## Learned Workspace Facts

- `velumia-pm/` is a git submodule at repo root (`git@github.com:liebsoer/velumia-pm.git`); tracked in `.gitmodules`; listed in `nx.json` `.nxignore`. Fresh clone: `git clone --recurse-submodules`; existing checkout: `git submodule update --init --recursive` (see `SETUP.md`).
- BDD Gherkin specs: `velumia-pm/bdd/`; local runner: `e2e/bdd/` (`pnpm nx run bdd:test`; CI matrix tags `@mock-langdock`, `@prompt-library` with **22 scenarios**). GitHub CI does **not** checkout `velumia-pm` (dev-only submodule; Bdd harness uses `e2e/bdd/`). Scenario **X-02** (trash confirm) covered by vitest `apps/ui/src/views/prompt-library-trash-confirm.test.ts` via BDD UI harness.
- **Stack (V1):** Tauri 2 + Vue 3 + SQLite; local-first desktop app for prompts, agents, and skills with LangDock BYOK; macOS dogfood.
- IPC contract: `docs/ipc.md`.
- **Sprint ceremony (ChatPRD-first):** Planning carry-over order: `retro-carryover.md` → `security-carryover.md` (open Critical/High need **stakeholder** fix/waiver/defer) → `architecture-carryover.md` (team first, escalate to stakeholder). Then Sprint PRD → refinement (**Architecture and security impact** mandatory) → Implementation Spec with § **Architecture and security impact** → `velumia-dev-security` + `velumia-dev-architect` Planning reviews. Before Review: both Implementation review sections + QA DoD. On-demand: `/security-review LIE-NNN`, `/architecture-review LIE-NNN`. Start with `/sprint-start LIE-NNN`. Retro `tooling` actions update agents/skills/`AGENTS.md` via **velumia-retro-tooling-sync**. `retro.md` and `retro-carryover.md` are git-tracked per sprint; other `.ai/velumia-sprints/LIE-*/` files stay ignored. See `.cursor/plans/delivery/velumia-sprint-ceremony.plan.md`.
- **Dev agents & Nx monorepo:** read skills `velumia-nx-monorepo` (layout, lib policy) and `velumia-dev-verify` (pre-handoff `pnpm nx` checks) before implementing or handing off. pnpm workspace with `apps/ui` (Vue/Vite), `apps/desktop` (Tauri/Rust), `e2e/bdd` (BDD harness); shared code in `libs/ui/*` and `libs/desktop/*` when extracted. First lib: `libs/ui/run-panel` (`@velumia/ui-run-panel`) — `RunPanel` + `RunPanelApi`; prompts wire via `apps/ui/src/lib/prompt-run-adapter.ts` (`createPromptRunApi`).
- **Toolchain:** Node **22.23.1** (`.node-version`, `.nvmrc`, `engines.node`); pnpm **10.12.1** (`packageManager`, `engines.pnpm`); CI reads `.node-version` and `packageManager` via `actions/setup-node` + `pnpm/action-setup`.
- Tauri `beforeDevCommand` / `beforeBuildCommand` run from **repo root** (`pnpm nx run ui:dev` / `ui:build`); standalone Vite web dev uses `web-api.ts` localStorage shim (`velumia.web-dev.v1`) with shared DTOs in `ipc-types.ts`. API keys are session-only in an in-memory `Map` (not localStorage); `webAuthorize` on protected paths. Authz test stubs: env `VELUMIA_AUTHZ_STUB_DENY` (write), `VELUMIA_AUTHZ_STUB_DENY_EXECUTE` (execute); web mirrors via `sessionStorage` with the same keys.
- **First-launch LangDock wizard:** `apps/ui/src/views/WizardView.vue` (credentials → optional starter samples); validate before step 2, show errors on all steps, busy state during connectivity test.
- **Prompt content editor & versioning:** CodeMirror 6 in `apps/ui`; per-prompt `content_syntax` (`auto` | `plaintext` | `markdown` | `xml` | `json`, migration `002_prompt_content_syntax.sql`); auto-detect from content with manual override; plain text when unrecognized. Linear history on content save (`prompt_versions` + `head_version_id`); client-side read-only diff; restore appends new head version; block restore identical to head; metadata/syntax edits do not bump version.
- **Prompt lifecycle (LIE-62):** `lifecycle_status` `active` | `archived` | `trashed`; IPC `archive_prompt`, `unarchive_prompt`, `restore_prompt`; `list_prompts` accepts `lifecycleFilter`; Active/Archived/Trash UI tabs; archived detail is read-only (view content/history, no edit/run).
- **Prompt runs (LIE-63):** IPC `start_prompt_run`, `send_prompt_message`, `stop_prompt_run`, `list_prompt_sessions`, `get_session_transcript`, `delete_prompt_session`; Tauri events `prompt-run-chunk`, `prompt-run-done`, `prompt-run-error`, `prompt-run-stopped`; LangDock `POST /agent/v1/chat/completions` streaming; default model from settings key `langdock_default_model`; `{{variable}}` substitution in Rust before send; variables modal in UI before first run; sessions + `transcript.jsonl` under app data; `prompt:execute` on run lifecycle, `prompt:read` on session list/transcript. Run UI in `libs/ui/run-panel`; async spawn pattern in `docs/ipc.md` § Async streaming.
